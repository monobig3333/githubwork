# 性能・可用性・非正常系試験 再測定 実行計画

- 作成日: 2026/7/31
- 契機: システム全体見直し（イベントルール KDI 版 → 改版）に伴う再測定
- 対象: `性能・可用性・非正常系テスト計画書.xlsx` で 実施日・結果 が空欄の項目
- 前回条件: **同条件で再実行**（比較可能性を確保）
- 対象インスタンス: **`biglobenonprod` に全項目を統一**（2026/7/31 決定。前回 biglobedev で実施した JMeter 系も nonprod へ寄せる）

---

## 1. 対象項目（13 件）

`OLD/性能・可用性・非正常系テスト計画書(イベントルールKDI版).xlsx` との差分を取った結果、
クリアされているのは **実施日 (J列) / 結果 (K列) のみ・計 14 セル対** で、
試験内容・判定基準の変更はない。

### 性能・可用性 テスト計画 シート（10 件）

| 行 | 要件No | テスト項目 | 前回実施日 | ツール |
|---|---|---|---|---|
| R4 | 1-1 | 画面応答時間（通常操作） | 2026/5/13 | Playwright |
| R5 | 1-2 | 同時接続 165（参照/更新） | 2026/5/18 | JMeter |
| R6 | 1-3 | 同時接続 330（参照専用） | 2026/5/18 | JMeter |
| R7 | 1-4 | チケット起票 1,000 件 | 2026/5/18 | Playwright |
| R8 | 2-1 | アラームビューワー 165 同時接続 | 2026/5/18 | JMeter |
| R9 | 2-2 | アラーム処理性能（30,000件/10分） | 2026/6/2 | Zabbix HTTPS API |
| R10 | 2-3 | イベント描画応答（通常時） | 2026/6/2 | Playwright + Zabbix |
| R11 | 2-4/5 | イベント描画応答（高負荷時） | 2026/6/3 | Playwright + Zabbix |
| R12 | 2-6 | イベント描画応答（高負荷継続30分） | 2026/6/3 | Playwright + Zabbix |
| R13 | 3-1 | ワークフロー並列 100 | 2026/5/18 | JMeter |

### MIDサーバ テスト計画 シート（3 件）

| 行 | 要件No | テスト項目 | 前回実施日 | ツール |
|---|---|---|---|---|
| R5 | M-1 | イベント転送スループット（通常時） | 2026/6/2 | JMeter |
| R6 | M-2 | イベント転送スループット（最大負荷） | 2026/6/2 | JMeter |
| R7 | M-3 | 高負荷継続時の性能飽和確認（30分） | 2026/6/2 | JMeter |

### 対象外

| 要件No | 理由 |
|---|---|
| M-10 | 外部試験（`Trap_負荷試験.xlsx`）。外部チーム実施のため本計画の対象外。前回実施日 2026/6/10・OK。**成果物の再取得要否は別途確認** |
| 上記以外 | 実施日・結果が入力済みのため再測定不要 |

---

## 2. 事前準備（実測前に必ず消化）

### 2-1. ブロッカー（未解消だと実測不可）

| # | 項目 | 現状 | 対応 |
|---|---|---|---|
| B1 | `auth.json` の期限切れ | 最終取得 **2026/6/5 09:49**、セッション cookie は同日 10:49 で失効済 | `python3 _common/save_auth_state.py` で再取得（Google SSO + MFA）。有効期間 約8時間なので **計測当日の朝に取得** |
| B2 | Zabbix 負荷投入ホストの生存 | `test-servicenow-monohyouka-00001`〜`30000` が前回作成分のまま残存しているか未確認 | `python3 2-2/zabbix_load.py --total 10 --dry-run` で確認。不足時は `zabbixtool/zabbix_bulk_copy_1.py` で再作成 |
| B3 | `ZABBIX_SCRIPT_ID` の有効性 | `.env` に設定済だが、見直しで script が作り直されている可能性 | `python3 2-2/zabbix_load.py --list-scripts` で scriptid を照合 |
| B4 | MID Server 3AZ の稼働 | 未確認 | `python3 N-common/verify_mid_status.py` もしくは ServiceNow `ecc_agent` で 3 台とも Up を確認 |
| B5 | **nonprod 用 OAuth client credentials** | **2026/7/31 実測で確定**: 1-2 のスモーク実行で `POST /oauth_token.do` が **401 Unauthorized**。setUp Thread Group の `on_sample_error=stoptest` によりメインのリクエストは 1 件も走らなかった。現行の client_id / secret は biglobedev 向けで、OAuth アプリケーションはインスタンス単位のため nonprod では認証できない | biglobenonprod 側で OAuth アプリケーション（`System OAuth → Application Registry` の *Create an OAuth API endpoint for external clients*）の client_id / secret を取得し、`jmeter.properties` を更新。あわせて `snow.basic_auth` を再生成（下記コマンド）|

```bash
# B5 疎通確認: jmeter.properties の実値を読んで叩く（$CID/$SEC の未設定事故を防ぐ）
CID=$(grep '^snow.client_id=' jmeter.properties | cut -d= -f2-)
SEC=$(grep '^snow.client_secret=' jmeter.properties | cut -d= -f2-)
curl -s -u "$CID:$SEC" -d 'grant_type=client_credentials' \
  https://biglobenonprod.service-now.com/oauth_token.do | jq .

# nonprod の client_id / secret を設定後、snow.basic_auth を再生成して jmeter.properties に反映
printf '%s:%s' "$CID" "$SEC" | base64 | tr -d '\n'
```

### 2-2. 要判断（実測前に方針を確定）

| # | 論点 | 内容 |
|---|---|---|
| ~~D1~~ | ~~JMeter 系の対象インスタンス~~ | **決着済（2026/7/31）**: 全項目を `biglobenonprod` に統一。反映内容は 2-4 節を参照 |
| D2 | **3-1 のトリガ URL** | `3-1_workflow_parallel.jmx` の path `/api/sn_ind_pmpf/workflow/trigger` は README 上「サンプル」。イベントルール見直しでフロー構成が変わっていれば path 修正が必要 |
| D3 | **2-2 の投入レート** | 前回実績は success 29,120 / 30,000、elapsed 1,701 秒（目標 600 秒）、実効 17.6 rps（目標 50 rps）で **Zabbix 側 read timeout により 880 件失敗**。同条件だと再現する見込み。判定基準「処理漏れ・スキップなし」に対する扱い（Zabbix 側限界としての注記運用）を事前合意 |
| D4 | **2-6 の評価区間** | 前回は後半15分に機器復帰イベントが混入したため「前半15分で判定」とした。今回も同様の外乱が入った場合の扱いを事前に決めておく |
| D5 | **1-4 の実施時間** | 1,000 件を Playwright で逐次起票。所要時間が最大。夜間バッチ的に単独枠を確保するか判断 |

### 2-3. ⚠️ 既存設定の落とし穴（重要）

#### (a) `-p` ではなく `-q` を使う（2026/7/31 修正）

JMeter の `-p` は **指定ファイルを既定の `jmeter.properties` の「代わりに」読み込む**オプション。
リポジトリの `jmeter.properties` は 15 行しかないため、`-p` を使うと `summariser.name` を含む
JMeter の既定設定がすべて失われる（実行中の `summary = ...` 進捗が出ない、
HTML レポート生成の既定も効かない）。

追加読み込みは `-q`（`--addprop`）。**本計画のコマンドはすべて `-q jmeter.properties` に統一済み。**

```bash
jmeter -n -t <jmx> -q jmeter.properties -J... -l <jtl>
```

前回計測（2026/5/18）は `-p` で実施しており結果自体は取得できているが、今回から `-q` に統一する。
`1-2/RESULT.md` `1-3/RESULT.md` `2-1/RESULT.md` の再実行手順は前回実績の記録としてそのまま残してある。

#### (b) プロパティのグローバル上書き

`jmeter.properties` はグローバルなプロパティファイルであり、`-q jmeter.properties` を付けると
**全 JMX の `__P()` 既定値を上書きする**。現在の値:

```
ramp_up=60      loop.count=10      threads.normal=165      threads.read_only=330      duration.sec=600
```

この結果、README / 前回 RESULT.md の条件と食い違う JMX がある:

| JMX | JMX 内の既定 | jmeter.properties 適用後 | 前回 RESULT.md 実績 | Excel 前提条件 | 対処 |
|---|---|---|---|---|---|
| 1-2 | ramp 60 / loop 10 | ramp 60 / loop 10 | ramp 60 / loop 10 | ランプアップ 30 秒 | 前回踏襲で **ramp 60** を明示。Excel 表記との差は RESULT.md に注記 |
| 1-3 | ramp 120 / loop 5 | **ramp 60 / loop 10** | ramp 120 / loop 10 / throughput 1200 | ランプアップ 60 秒 | 前回踏襲で `-Jramp_up=120 -Jloop.count=10 -Jthroughput=1200` を明示 |
| 2-1 | ramp 60 / loop 10 | ramp 60 / loop 10 | ramp 60 / loop 10 / throughput 1200 | ランプアップ 30 秒 | `-Jthroughput=1200` を明示 |
| 3-1 | ramp 10 / loop 1 | **ramp 60 / loop 10** | （RESULT.md なし） | 100 プロセス同時起動 | `-Jthreads=100 -Jramp_up=10 -Jloop.count=1` を明示 |
| M-1 | threads 10 / ramp 5 / loop 30 | **threads 165 / ramp 60 / loop 10** | （RESULT.md なし） | 標準負荷 | README の 10 スレッド × 30 loop に合わせ `-Jthreads=10 -Jramp_up=5 -Jloop.count=30` を明示 |
| M-2 | threads 50 / ramp 10 / duration 600 | **threads 165 / ramp 60** / duration 600 | （RESULT.md なし） | 30,000件/10分 | `-Jthreads=50 -Jramp_up=10 -Jduration.sec=600` を明示 |
| M-3 | threads 50 / ramp 30 / duration 1800 | **threads 165 / ramp 60 / duration 600** | （RESULT.md なし） | 30 分継続 | `-Jthreads=50 -Jramp_up=30 -Jduration.sec=1800` を明示 |

> Excel の「ランプアップ」表記と前回実績が食い違う項目（1-2 / 1-3 / 2-1）がある。
> 「同条件で再実行」の方針に従い **前回 RESULT.md の実績条件を正** とし、
> Excel 表記との差は各 RESULT.md に注記する。

→ **本計画のコマンドはすべて `-J` で明示指定する。** 暗黙の既定値に依存しない。

### 2-4. インスタンス統一（biglobenonprod）の反映内容

前回 JMeter 系は `biglobedev` で実施していたが、今回は **全項目を `biglobenonprod` に統一**する。
以下は 2026/7/31 時点で反映済み。

| 対象 | 変更前 | 変更後 |
|---|---|---|
| `jmeter.properties` | `snow.host=biglobedev.service-now.com` | `snow.host=biglobenonprod.service-now.com` |
| `jmeter.properties.example` | 同上 | 同上 |
| JMX 7 本の `${__P(snow.host, ...)}` 既定値<br>（1-2 / 1-3 / 2-1 / 3-1 / M-1 / M-2 / M-3） | `biglobedev.service-now.com` | `biglobenonprod.service-now.com` |
| `2-2/README.md` の受信確認コマンド `HOST=` | `biglobedev.service-now.com` | `biglobenonprod.service-now.com` |
| `.env` | `SNOW_BASE_URL=https://biglobenonprod.service-now.com` | 変更なし（元から nonprod） |

**意図的に変更していないもの**

| 対象 | 理由 |
|---|---|
| `2-2` / `2-4-5` / `2-6` の旧 JMX | Zabbix 外部投入に統合済みで今回使用しない（各 README に「使用しない方針」の記載あり）|
| `M-5` / `M-6` の JMX、`13-1` の JMX | 今回の再測定対象外（実施済み / スコープ外）|
| `1-2` / `1-3` / `2-1` の既存 `RESULT.md` | 前回実績（biglobedev）の記録として保全。今回分は節を追記する |

> `-q jmeter.properties` を付ける限り JMX 内のフォールバック既定値は使われないが、
> 事故防止のため対象 7 本は既定値も nonprod に揃えてある。
> `preflight_check.py` の env チェックがこの 7 本を検証する。

**残作業（手動）**

1. **B5**: nonprod 用 OAuth client_id / secret を取得して `jmeter.properties` を更新（+ `snow.basic_auth` 再生成）
2. **テストアカウント**: 1-2（165）/ 1-3（330）/ 2-1（165）で使うアカウントが nonprod 側にも存在するか確認
3. **データ量の差**: nonprod と dev で incident / em_alert の件数が違うと応答時間が変わる。前回値との比較時は
   データ量の差を RESULT.md に明記する
4. `1-2/RESULT.md` `1-3/RESULT.md` `2-1/RESULT.md` の「対象環境: biglobedev」は**前回実績の記録**として
   そのまま残し、今回分は「## 再測定 2026/7/31 (biglobenonprod)」節として追記する

> ⚠️ インスタンスが変わるため、1-2 / 1-3 / 2-1 / 3-1 は **厳密には「同条件」ではない**。
> 前回値は参考値として扱い、判定は Excel の合否判定基準（絶対値）で行う。

---

## 2-5. 実行環境の役割分担（2026/7/31 決定）

**全 13 件を mono さんが Mac 上で手動実行する。**

| 担当 | 内容 |
|---|---|
| mono さん | 4 章の手順に沿ってコマンドを実行し、`result*.json` / JMeter の `.jtl` を生成 |
| Claude | 出力ファイルの解析、判定、各 `RESULT.md` の作成・更新、Excel（実施日・結果）への反映、評価報告書の再生成 |

計測が終わったら、生成された結果ファイルのパスを伝えるか、標準出力を貼り付けてください。
リポジトリはマウント済みなので、`result*.json` / `.jtl` は Claude が直接読めます。

### 自動実行を検討した経緯（記録）

Cowork サンドボックスからの自動実行も検討したが、以下の理由で見送った。

| 方式 | 判断 |
|---|---|
| サンドボックスで直接実行 | ネットワーク許可リスト（[Organization settings > Capabilities](https://claude.ai/admin-settings/capabilities) の Code execution）に `*.service-now.com` と `storage.googleapis.com`（Chromium 取得先）の追加が必要。組織 Owner 権限が要り、GCS 全体の開放を伴うため見送り |
| Claude in Chrome | 実ブラウザのログイン済みセッションを引き継ぐため、社内システムへの到達範囲が広がる。専用プロファイルでも Google SSO 経由で Google セッションが乗るため見送り |
| Zabbix 連携（2-2 / 2-4/5 / 2-6） | `10.249.73.66` は社内 private IP。許可リストでは解決せず、そもそも自動化不可 |

> 2026/7/31 実測時点でサンドボックスから到達できたのは `pypi.org` / `files.pythonhosted.org` /
> `registry.npmjs.org` / `crates.io` / `archive.ubuntu.com` / `github.com` /
> `objects.githubusercontent.com` / Anthropic 系のみ。`*.service-now.com` は遮断。

---

## 3. 実行順序と所要時間

負荷の軽い順・依存関係順に 4 フェーズへ分割。フェーズ間で環境の落ち着き（5〜10分）を取る。

| Phase | 項目 | 想定所要 | 必要端末 |
|---|---|---|---|
| **P0 準備** | preflight / auth.json 取得 | 30 分 | 端末A |
| **P1 JMeter 軽負荷** | 1-2 → 1-3 → 2-1 → 3-1 | 30 分 | 端末A |
| **P2 Playwright 単独** | 1-1 → 1-4 | 5 分 + 60〜120 分 | 端末A |
| **P3 MID 転送系** | M-1 → M-2 → M-3 | 5 + 15 + 35 = 55 分 | 端末A（+ MID ログ観測） |
| **P4 Zabbix 連携** | 2-2 → 2-3 → 2-4/5 → 2-6 | 40 + 15 + 20 + 40 = 115 分 | 端末A（計測）+ 端末B（Zabbix 投入）|

**合計目安: 約 5〜7 時間（1-4 の実測次第で 1〜1.5 日）**

> P4 は 2 端末同期が必要。`Enter で計測開始 >` プロンプトで端末A/B を合わせる。
> auth.json の有効期間が約 8 時間のため、P2 以降が翌日にまたぐ場合は再取得すること。

---

## 4. 項目別 実行手順

すべて `cd /Users/bx0815610/githubwork/work/servicenow/kakusyou` から実行。
**対象インスタンスは全項目 `biglobenonprod.service-now.com`**（JMeter は `jmeter.properties` の `snow.host`、
Playwright / Zabbix 系は `.env` の `SNOW_BASE_URL` で解決される）。

### P0 準備

```bash
source .venv/bin/activate
pip install -r requirements.txt

# 事前チェック（下記 5 章の preflight を利用）
python3 _common/preflight_check.py

# auth.json 再取得（Google SSO + MFA）
python3 _common/save_auth_state.py
python3 2-3/api_probe.py          # 生存確認
```

---

### P1-1 / 要件 1-2 — 同時接続 165（参照/更新）

```bash
jmeter -n -t 1-2/1-2_concurrent_165.jmx -q jmeter.properties \
  -Jthreads=165 -Jramp_up=60 -Jloop.count=10 \
  -l 1-2/runs/run_20260731_$(date +%H%M%S).jtl
```

- 判定: HTTP エラー 0 件 / 応答時間 3 秒以内
- 前回値（**biglobedev** 実施・参考）: 平均 1,375ms / 99%tile 2,818ms / 3秒超過 3件 (0.18%) → 合格（実用判定）
- レポート再生成: `jmeter -g 1-2/runs/run_*.jtl -o 1-2/report -Jjmeter.reportgenerator.apdex_satisfied_threshold=1000 -Jjmeter.reportgenerator.apdex_tolerated_threshold=3000`

### P1-2 / 要件 1-3 — 同時接続 330（参照専用）

```bash
jmeter -n -t 1-3/1-3_concurrent_330_readonly.jmx -q jmeter.properties \
  -Jthreads=330 -Jramp_up=120 -Jloop.count=10 -Jthroughput=1200 \
  -l 1-3/runs/run_20260731_$(date +%H%M%S).jtl
```

- 判定: エラー 0 件 / 接続拒否・タイムアウトなし
- 前回値（**biglobedev** 実施・参考）: 3,300 req 全件 200、平均 323ms / 最大 1,231ms
- ⚠️ Zscaler ZIA の 429 回避のため Constant Throughput Timer 1,200/分 必須

### P1-3 / 要件 2-1 — アラームビューワー 165 同時接続

```bash
jmeter -n -t 2-1/2-1_alarm_viewer_165.jmx -q jmeter.properties \
  -Jthreads=165 -Jramp_up=60 -Jloop.count=10 -Jthroughput=1200 \
  -l 2-1/runs/run_20260731_$(date +%H%M%S).jtl
```

- 判定: エラー 0 件 / 画面（API）が正常応答
- 前回値（**biglobedev** 実施・参考）: 1,650 req 全件 200、平均 390ms / 最大 1,634ms

### P1-4 / 要件 3-1 — ワークフロー並列 100

```bash
# ★ 事前に D2（トリガ URL）を確定してから実行
jmeter -n -t 3-1/3-1_workflow_parallel.jmx -q jmeter.properties \
  -Jthreads=100 -Jramp_up=10 -Jloop.count=1 \
  -l 3-1/runs/run_20260731_$(date +%H%M%S).jtl -e -o 3-1/report/
```

- 判定: 100 プロセス並列実行、プロセス間の干渉・エラーなし

---

### P2-1 / 要件 1-1 — 画面応答時間（通常操作）

```bash
pytest 1-1/ -v            # 出力: 1-1/result_1_1.json
```

- 判定: 全画面で平均 3 秒以内
- 前回値: 一覧 avg 1.21s / 詳細 avg 1.62s / 変更登録 avg 1.51s（max はいずれも 3秒超えあり → **平均で判定**）

### P2-2 / 要件 1-4 — チケット起票 1,000 件

```bash
# まず小件数で疎通（TICKET_COUNT を一時的に 5 に変更 or 環境変数化）
pytest 1-4/ -v -s         # 出力: 1-4/result_1_4.json
```

- 判定: 1,000 件全件成功・欠損/重複なし
- ⚠️ 最長ジョブ。単独枠で実施推奨（D5）

---

### P3-1 / 要件 M-1 — MID 転送スループット（通常時）

```bash
# 端末A
jmeter -n -t M-1/M-1_mid_throughput_normal.jmx -q jmeter.properties \
  -Jthreads=10 -Jramp_up=5 -Jloop.count=30 \
  -l M-1/result.jtl -e -o M-1/report/

# 並行して MID ログ観測（別ペイン）
for h in $(grep '^MID_HOSTS=' .env | cut -d= -f2 | tr ',' ' '); do
  ssh midserver@$h 'tail -f /opt/midserver/agent/logs/agent0.log.0' | tee -a M-1/logs_$h.log &
done
```

- 判定: 転送完了まで **10分30秒以内**（Excel 記載の SLA）/ 転送漏れなし
- ⚠️ README には「3分30秒以内」とあり Excel と不一致。**Excel（10分30s）を正とする**

### P3-2 / 要件 M-2 — MID 転送スループット（最大負荷 30,000件/10分）

```bash
jmeter -n -t M-2/M-2_mid_throughput_max.jmx -q jmeter.properties \
  -Jthreads=50 -Jramp_up=10 -Jduration.sec=600 \
  -l M-2/result.jtl -e -o M-2/report/

# 突合
grep -c ',true,' M-2/result.jtl
# ServiceNow 側件数（OAuth token 取得後）
# GET /api/now/table/em_event?sysparm_query=source=mid-test-M-2&sysparm_count=true
```

- 判定: 全イベント転送、処理漏れ・スキップなし

### P3-3 / 要件 M-3 — 高負荷継続 30 分（性能飽和確認）

```bash
jmeter -n -t M-3/M-3_mid_sustained_load.jmx -q jmeter.properties \
  -Jthreads=50 -Jramp_up=30 -Jduration.sec=1800 \
  -l M-3/result.jtl -e -o M-3/report/

# 5分窓での時系列分析
python3 - <<'PY'
import pandas as pd
df = pd.read_csv('M-3/result.jtl')
df['t'] = pd.to_datetime(df['timeStamp'], unit='ms')
df.set_index('t', inplace=True)
print(df['elapsed'].resample('5min').agg(['mean','max','count']))
PY
```

- 判定: 転送遅延が増加し続けないこと（性能飽和なし）/ 処理漏れなし

---

### P4-1 / 要件 2-2 — アラーム処理性能（30,000件/10分）

```bash
# 1) 疎通
python3 2-2/zabbix_load.py --list-scripts
python3 2-2/zabbix_load.py --total 10 --dry-run
python3 2-2/zabbix_load.py --total 10 --rate 5

# 2) 本番
python3 2-2/zabbix_load.py --total 30000 --rate 50   # 出力: 2-2/result_2_2.json
```

- 判定: API 成功率 100% / ServiceNow em_event に全件受信
- 前回値: success 29,120 / failed 880（Zabbix read timeout）、elapsed 1,701s、実効 17.6 rps
- ⚠️ D3 の方針を事前確定のうえ実施

### P4-2 / 要件 2-3 — イベント描画応答時間（通常時）

```bash
# 端末A
pytest 2-3/ -v -s          # -s 必須。出力: 2-3/result_2_3.json
# 端末B: Zabbix 通常流量を投入 → 端末A のプロンプトで Enter
```

- 判定: 描画完了まで **3 秒以内**、20 回計測
- ⚠️ 実行端末と ServiceNow の NTP 同期を確認（時計ずれが直接誤差）

### P4-3 / 要件 2-4/5 — イベント描画応答時間（高負荷時）

```bash
# 端末A
pytest 2-4-5/ -v -s        # 既定 600s / 50件。出力: 2-4-5/result_2_4_5.json
# 短縮ドライラン: PERF_DURATION_SEC=300 PERF_MAX_ITER=30 pytest 2-4-5/ -v -s

# 端末B
python3 2-2/zabbix_load.py --total 30000 --rate 50
```

- 判定: 平均 60 秒以内 / 最大 180 秒以内
- 前回値: 平均 **59.60s**（閾値 60s に対し余裕 0.4s）/ 最大 109.67s → **最も再現リスクが高い項目**

### P4-4 / 要件 2-6 — イベント描画応答時間（高負荷継続 30 分）

```bash
# 端末A
pytest 2-6/ -v -s          # 既定 1800s / 150件。出力: 2-6/result_2_6.json

# 端末B: 30 分継続投入
python3 2-2/zabbix_load.py --total 90000 --rate 50
```

- 判定: 平均 60 秒以内 / 最大 180 秒以内 / 性能飽和なし（後半平均 ÷ 前半平均 ≤ 1.5）
- 前回値: 前半15分 avg 37.45s / max 117.80s（評価対象）、飽和比 1.525
- ⚠️ D4 の方針を事前確定

---

## 5. 事前チェックスクリプト

`_common/preflight_check.py` を追加。以下を一括確認する。

```bash
python3 _common/preflight_check.py                 # 全チェック
python3 _common/preflight_check.py --only zabbix   # 一部だけ
python3 _common/preflight_check.py --skip oauth
```

負荷は一切かけない read-only チェック。FAIL が残っている状態では実測に進まないこと。

| チェック | 内容 |
|---|---|
| env | `.env` の必須キーが揃っているか |
| auth | `auth.json` の存在・セッション cookie 有効期限 |
| snow | ServiceNow へ cookie + X-UserToken で `em_event` を 1 件取得できるか |
| oauth | `oauth_token.do` で client_credentials が通るか |
| zabbix | Zabbix API 疎通 / scriptid の存在 / 負荷用ホスト件数 |
| mid | `ecc_agent` の 3 台が Up か |
| tools | jmeter / java / python パッケージの導入状況 |

---

## 6. 結果の記録ルール

1. 各要件ディレクトリに `result*.json` を出力（gitignore 対象・コミットしない）
2. 各要件ディレクトリの `RESULT.md` を **今回分に更新**（前回分は `RESULT_20260603.md` 等へ退避、または「## 再測定 2026/7/31」節を追記）
3. `性能・可用性・非正常系テスト計画書.xlsx` に記入
   - 実施日 (J列): **文字列で `YYYY/M/D`**（ゼロ埋めなし・スラッシュ区切り）
   - 結果 (K列): `OK` / `NG` / `△` / `参考`
   - 対象セル: 性能シート R4〜R13 / MID シート R5〜R7
4. 全件確定後に `build_report.js` で評価報告書 pptx / pdf を再生成
5. コミットは `[要件No] 動作概要` 形式

---

## 7. リスクと事前対策

| リスク | 影響項目 | 対策 |
|---|---|---|
| 2-4/5 の平均が 60 秒を超える | 2-4/5 | 前回 59.60s と余裕僅少。NG 時は Zabbix バーストパターンを RESULT.md に記録し、要件チームと計測条件を再合意 |
| Zabbix 側 read timeout で投入レートが出ない | 2-2 / 2-4/5 / 2-6 | `--parallel` を調整、または投入を分割。既知事象（バースト時取りこぼし 0.02%）として注記 |
| Zscaler の 429 | 1-3 / 2-1 | Constant Throughput Timer 1,200/分 を必ず指定 |
| auth.json の 8 時間失効 | 1-1 / 1-4 / 2-3 / 2-4/5 / 2-6 | 各フェーズ開始前に `2-3/api_probe.py` で生存確認 |
| イベントルール改版で em_event の node/source 書式が変化 | 2-3 / 2-4/5 / 2-6 | P4 開始前にビューワーで 1 件手動確認し、DOM 検知キー（sys_id ベース）が機能するかスモークで検証 |
| 後半への外乱イベント混入 | 2-6 | 計測時間帯を機器復帰の少ない時間に設定。混入時は前回同様に区間を分けて評価 |
| nonprod 移行で OAuth が通らない | 1-2 / 1-3 / 2-1 / 3-1 / M-1〜M-3 | B5 の対応が前提。preflight の oauth チェックで事前検出 |
| nonprod にテストアカウント（165 / 330 件）が無い | 1-2 / 1-3 / 2-1 | JMeter は OAuth Bearer 単一トークンで動作するため必須ではないが、シナリオ内でユーザ切替をしている場合は要確認 |
| dev と nonprod のデータ量差で応答時間が変動 | 1-2 / 1-3 / 2-1 / 3-1 | 前回値との単純比較は避け、Excel の絶対値基準で判定。データ件数を RESULT.md に記録 |

---

## 8. 実行チェックリスト

手動実行用。上から順に消化する。詳細コマンドは 4 章参照。

### 準備

- [ ] `source .venv/bin/activate` / `pip install -r requirements.txt`
- [ ] **B5**: nonprod の OAuth client_id / secret を `jmeter.properties` に設定し `snow.basic_auth` を再生成
- [ ] `python3 _common/save_auth_state.py` → `python3 2-3/api_probe.py`
- [ ] `python3 _common/preflight_check.py` が FAIL 0 になること

### P1 JMeter 軽負荷（約 30 分）

- [ ] 1-2 `-Jthreads=165 -Jramp_up=60 -Jloop.count=10`
- [ ] 1-3 `-Jthreads=330 -Jramp_up=120 -Jloop.count=10 -Jthroughput=1200`
- [ ] 2-1 `-Jthreads=165 -Jramp_up=60 -Jloop.count=10 -Jthroughput=1200`
- [ ] 3-1 `-Jthreads=100 -Jramp_up=10 -Jloop.count=1` ※ D2 のトリガ URL 確定後

### P2 Playwright 単独（約 1〜2 時間）

- [ ] 1-1 スモーク `PERF_ITERATIONS=2 pytest 1-1/ -v -s --headed`
- [ ] 1-1 本番 `pytest 1-1/ -v`
- [ ] 1-4 `pytest 1-4/ -v -s`（1,000 件・最長ジョブ）

### P3 MID 転送系（約 55 分）

- [ ] M-1 `-Jthreads=10 -Jramp_up=5 -Jloop.count=30`
- [ ] M-2 `-Jthreads=50 -Jramp_up=10 -Jduration.sec=600` + 送受信突合
- [ ] M-3 `-Jthreads=50 -Jramp_up=30 -Jduration.sec=1800` + 5 分窓の時系列分析

### P4 Zabbix 連携（約 115 分・2 端末同期）

- [ ] 2-2 `python3 2-2/zabbix_load.py --total 30000 --rate 50`
- [ ] 2-3 端末A `pytest 2-3/ -v -s` / 端末B 通常流量
- [ ] 2-4/5 端末A `pytest 2-4-5/ -v -s` / 端末B 30,000件/10分
- [ ] 2-6 端末A `pytest 2-6/ -v -s` / 端末B 30 分継続

### 反映（Claude 担当）

- [ ] 各 `RESULT.md` の作成・更新
- [ ] Excel 記入（性能シート R4〜R13 / MID シート R5〜R7、実施日は `YYYY/M/D` 文字列）
- [ ] `build_report.js` で pptx / pdf 再生成
- [ ] コミット（`result*.json` は除外）
